import 'package:flutter/material.dart';

Widget buildWebVideoPlayer(String url, double width, double height) {
  return const Center(child: Text('Web video player is not supported on this platform'));
}
